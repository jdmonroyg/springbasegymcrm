server:
  port: 8080
  servlet:
    context-path: /api/v1
spring:
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
  endpoint:
    health:
      show-details: always
jwt:
  secret: ${JWT_KEY}
